---
title: Starlight components
description: Every Starlight built-in component as restyled by Exquisitus — a complete visual reference.
sidebar:
  order: 3
pagefind: false
---

import { Aside, Tabs, TabItem, Card, CardGrid, LinkCard, Steps, Badge, LinkButton, FileTree } from '@astrojs/starlight/components';

This page hosts a visual reference for every Starlight built-in component, rendered in the Exquisitus style. Each component keeps its own behavior and markup; the theme only changes how it looks.

## Asides

Four semantic variants, each with a distinct hue and a softened full border instead of a side stripe.

<Aside type="note">A note aside: calm petrol-teal, the information register. Set apart from prose, not competing with it.</Aside>

<Aside type="tip" title="A considered tip">A tip aside: violet, distinct from both the warm accent and the cool note so neither blurs into the other.</Aside>

<Aside type="caution">A caution aside: amber-orange, deliberately separated from the brand's link color so urgency and navigation never blend.</Aside>

<Aside type="danger">A danger aside: measured red, unmistakable without being lurid. It passes contrast on its own tint.</Aside>

## Tabs

<Tabs>
	<TabItem label="Light">The active tab is marked three ways at once: accent color, weight 600, and a 3px indicator. Selection is unambiguous without competing chrome. Light mode keeps a pure-white page.</TabItem>
	<TabItem label="Dark">In dark mode the accent brightens from bronze to honey, and the petrol-teal lifts to match. Both modes are validated to WCAG 2.2 AA.</TabItem>
	<TabItem label="A third tab">A resting baseline runs below the whole tab strip, so the indicator reads clearly as "selected" rather than as a random underline. Tabs are documentation-register: underline, not pill.</TabItem>
</Tabs>

## Steps

<Steps>

1. Step counters are set in JetBrains Mono with tabular figures, so multi-digit sequences stay aligned.

2. The connecting guide fades into the page rather than ending in a hard stop.

   ```js title="example.js"
   const step = 2;
   ```

3. Each step is free to hold prose, code, or another component.

</Steps>

## Cards

<CardGrid>
	<Card title="Hairline, not box" icon="approve-check">
		Cards are defined by a single hairline and a generous radius. On hover they lift two pixels and warm their border for a small, earned reward, suppressed on touch.
	</Card>
	<Card title="Serif titles" icon="pencil">
		Card titles are set in Spectral, tying them to the document headings rather than the interface chrome.
	</Card>
</CardGrid>

<LinkCard
	title="Read the typography page"
	description="See how prose, blockquotes, and tables read in the reading column."
	href="/showcase/typography/"
/>

## Link buttons

<LinkButton href="#_" icon="rocket">Primary</LinkButton>
<LinkButton href="#_" variant="secondary" icon="external">Secondary</LinkButton>
<LinkButton href="#_" variant="minimal" icon="external">Minimal</LinkButton>

The primary button uses the full honey-amber as a fill with an ink label—the one place in light mode where the brand color appears at full strength. It can't carry link text on white (insufficient contrast), so links deepen to bronze; the button, with its dark ink label, carries it freely.

## File tree

An open hairline figure rather than a filled box. It sits on the page surface and is pressed slightly into it with the same letterpress as the code block below, so the two read as one family without twinning. Petrol-teal marks structure—folder icons, disclosure markers, and the indent guides—while files stay neutral and the warm accent is reserved for hover and the highlighted file.

<FileTree>

- astro.config.mjs
- package.json
- src
  - content
    - docs
      - **getting-started.mdx**
      - index.mdx
  - styles
    - custom.css

</FileTree>

## Details

<details>
<summary>The summary of a disclosure</summary>

The content of the disclosure. It can hold any Markdown, whether prose, code blocks, or even lists. The summary is set in Spectral, tying it to the heading hierarchy.

</details>

## Code blocks

Code blocks are reframed as editorial figures. The filename becomes a quiet monospace caption above the panel—no editor tab, no traffic-light dots, no terminal costume—and the panel itself is a bordered, petrol-tinted surface pressed into the page. That letterpress is deliberate: code, like the file tree, belongs to the *information register*, marked on three axes at once—petrol-teal, monospace, and a debossed depth that sets it apart from the flat reading surface. Diff lines keep their familiar bands but also carry a `+`/`−` gutter glyph, so the change reads without relying on color alone.

```ts title="astro.config.mjs" ins={[2, 8]}
import starlight from '@astrojs/starlight';
import starlightThemeExquisitus from 'starlight-theme-exquisitus';
import { defineConfig } from 'astro/config';

export default defineConfig({
  integrations: [
    starlight({
      plugins: [starlightThemeExquisitus()],
      title: 'My Docs',
    }),
  ],
});
```

```diff lang="js"
  function greet(name) {
-   return 'Hello, ' + name;
+   return `Hello, ${name}`;
  }
```

```sh title="Terminal"
pnpm add starlight-theme-exquisitus
```

## Badges

Of course, badges get represented here.

<Badge text="New" variant="success" />, <Badge text="Beta" variant="caution" />, <Badge text="Note" variant="note" />, <Badge text="Deprecated" variant="danger" />, and <Badge text="Experimental" variant="tip" />.
